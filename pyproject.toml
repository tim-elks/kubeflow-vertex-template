[project]
name = "kubeflow-vertex-template"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "click>=8.0.0",
    "kfp==2.7.0",
    "google-cloud-aiplatform>=1.42.0",
    "pandas>=2.0.0",
    "scikit-learn>=1.3.0",
]

[tool.uv]
package = false
